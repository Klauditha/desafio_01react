import Badge from "react-bootstrap/Badge";

/* eslint-disable react/prop-types */
const Tags = ({ colorfondo, textotag }) => {
  return <Badge variant={colorfondo}>{this.textotag}</Badge>;
};

export default Tags;
